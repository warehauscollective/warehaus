'use client';

/**
 * Re-export from `@warehaus/ui` — swipe-tab behavior is shared brand chrome
 * for website + portal. Prefer importing from `@warehaus/ui` directly.
 */
export {
  useSwipeTabs,
  type UseSwipeTabsOptions,
} from '@warehaus/ui';
